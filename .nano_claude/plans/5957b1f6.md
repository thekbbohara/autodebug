# Implementation Plan: Fix All Bugs + Add All Missing Features

## Overview
Fix 3 known bugs and implement 8 missing features across `scripts/repo_audit.py`, `skills/autodebug.md`, `scripts/test_skill.py`, and related files.

---

## BUGS TO FIX

### Bug 1: repo_audit.py has 10 phases but autodebug.md has 11
**File**: `scripts/repo_audit.py`
**Fix**: Add `11_write_test_cases` phase to the `init_audit` phases dict. Update `generate_solution_plan` to reference the correct final phase key.

### Bug 2: No deduplication — same finding written on every iteration
**File**: `skills/autodebug.md` (skill prompt), `scripts/repo_audit.py`
**Fix**: 
- Add a `findings_index` tracking mechanism in `.loop_state` — store `{file_stem: severity}` for all written findings
- In `repo_audit.py`, add `add_finding` dedup by checking `findings.json` for existing `file_path + line + category + title` combination before writing
- In `autodebug.md` Phase instructions: add a rule to check `.loop_state` findings_index before writing a duplicate .md file

### Bug 3: runner.py and repo_audit.py are standalone CLIs not integrated with autodebug.md flow
**File**: `skills/autodebug.md` (skill prompt)
**Fix**: Add explicit integration instructions in SETUP section telling the AI to:
- Call `runner.py register` after SETUP, pass the audit ID
- Call `repo_audit.py init` after SETUP, use the same ID
- Call `repo_audit.py phase <id> <phase_key> in_progress/complete` at start/end of each phase
- Call `repo_audit.py finding <id> ...` when writing each finding
- Call `runner.py update <id>` after each full iteration
- Reference the audit ID in `.loop_state`

---

## FEATURES TO ADD

### Feature 1: Severity threshold (`--min-severity`)
**Files**: `skills/autodebug.md`, `scripts/repo_audit.py`
**Changes**:
- Add `min_severity` argument to `autodebug.md` frontmatter: `arguments: [focus, scope, write_testcase, min_severity]`
- Default: `low` (show everything). Options: `critical`, `high`, `medium`, `low`
- Add `## SEVERITY THRESHOLD` section explaining the filter
- In `repo_audit.py`: add `--min-severity` filter to `findings` command
- In each scan phase: add instruction to skip writing if severity is below threshold

### Feature 2: Ignore list (won't-fix)
**Files**: `skills/autodebug.md`, `scripts/repo_audit.py`
**Changes**:
- Add `debug_output/.ignore_list.json` — array of `{file_pattern, category, title_pattern}` entries
- In `repo_audit.py`: add `ignore` command to add entries, `unignore` to remove
- In `repo_audit.py`: `add_finding` checks ignore list before writing
- In `autodebug.md` SETUP: read `.ignore_list.json` if exists
- In `autodebug.md` scan phases: skip findings matching ignore patterns
- Add `## IGNORE LIST` section documenting usage

### Feature 3: Notification on critical findings
**Files**: `skills/autodebug.md`
**Changes**:
- Add `webhook_url` argument to frontmatter: `arguments: [focus, scope, write_testcase, min_severity, webhook_url]`
- When a `critical` finding is written, fire a webhook POST with JSON payload: `{severity, category, title, file, description}`
- Add `## NOTIFICATIONS` section
- Use `Bash` with `curl` to POST (already in tools list)

### Feature 4: Diff between iterations
**Files**: `skills/autodebug.md`, `scripts/repo_audit.py`
**Changes**:
- In `repo_audit.py`: add `snapshot <id>` command that saves current findings list to `audits/<id>/iteration_N.json`
- In `repo_audit.py`: add `diff <id> <iter_a> <iter_b>` command that compares two snapshots
- In `autodebug.md` Phase 10 (REGRESSION): 
  - Call `repo_audit.py snapshot` before looping
  - Call `repo_audit.py diff` to compare current vs previous iteration
  - Write `debug_output/NNN-iteration-diff.md` with new/resolved/changed findings

### Feature 5: Incremental indexing
**File**: `skills/autodebug.md`
**Changes**:
- In SETUP: on first iteration, do full `index_folder`. Store the index time in `.loop_state`
- In Phase 10 before looping: call `register_edit` with any files that were written/modified during the scan
- On subsequent iterations: use `index_folder` with `incremental=true` instead of full re-index
- Add instruction in CRITICAL RULES

### Feature 6: Parallel scanning with sub-agents
**File**: `skills/autodebug.md`
**Changes**:
- Add `parallel` argument to frontmatter: `arguments: [focus, scope, write_testcase, min_severity, webhook_url, parallel]`
- When `parallel` is passed, spawn independent sub-agents for independent phases:
  - Agent 1: Phases 2+4 (dead-code + dependencies)
  - Agent 2: Phases 5+7 (security + type safety)  
  - Agent 3: Phases 3+8 (hotspots + performance)
  - Agent 4: Phase 9 (DB scan — independent)
  - Main agent: Phase 1 (recon) + Phase 6 (logic — needs hotspot data) + Phase 10 (regression)
- After all sub-agents complete, merge findings and continue to Phase 11
- Add `## PARALLEL MODE` section

### Feature 7: Docker/container scanning phase
**Files**: `skills/autodebug.md`, `scripts/test_skill.py`
**Changes**:
- Add `### Phase 9.5: DOCKER SCAN` between Phase 9 and Phase 10
- Renumber: old Phase 10 → Phase 11, old Phase 11 → Phase 12
- Use `search_text` for Dockerfile patterns, `get_file_content` for Dockerfiles
- Check: running as root, no health checks, `ADD` instead of `COPY`, `latest` tags, exposed privileged ports, `chmod 777`, secrets in ENV
- Include in focus modes: `all`, `security`
- Update focus mode table
- Update `test_skill.py` required_sections

### Feature 8: API schema scanning phase
**Files**: `skills/autodebug.md`, `scripts/test_skill.py`
**Changes**:
- Add `### Phase 8.5: API CONTRACT SCAN` between Phase 8 and Phase 9.5
- Renumber accordingly (this shifts everything after Phase 8)
- Actually, simpler: insert after DB SCAN, before DOCKER SCAN as Phase 10, making:
  - Phase 10: API CONTRACT SCAN
  - Phase 11: DOCKER SCAN
  - Phase 12: REGRESSION CHECK
  - Phase 13: WRITE TEST CASES
- Scan: route definitions, missing input validation, missing auth decorators, inconsistent response shapes
- Use `search_text` for route patterns, `get_symbol_source` on handlers
- Include in focus modes: `all`, `security`
- Update focus mode table
- Update `test_skill.py` required_sections

**Final phase numbering**:
| Phase | Name |
|-------|------|
| 1 | RECON |
| 2 | DEAD CODE |
| 3 | HOTSPOTS |
| 4 | DEPENDENCIES |
| 5 | SECURITY SCAN |
| 6 | LOGIC BUGS |
| 7 | TYPE SAFETY |
| 8 | PERFORMANCE |
| 9 | DB SCAN |
| 10 | API CONTRACT SCAN |
| 11 | DOCKER SCAN |
| 12 | REGRESSION CHECK |
| 13 | WRITE TEST CASES |

---

## FILES MODIFIED (in order)

1. **`scripts/repo_audit.py`** — Add Phase 11+12+13 to init, add dedup in add_finding, add ignore/unignore/snapshot/diff commands, add --min-severity filter to findings
2. **`skills/autodebug.md`** — Add new arguments, new phases, dedup rules, ignore list, notifications, incremental indexing, parallel mode, updated phase numbers and focus map
3. **`scripts/test_skill.py`** — Add new phases to required_sections, add tests for repo_audit.py new commands
4. **`CHEETAHCLAWS.md`** — Update to reflect all changes
5. **`README.md`** — Update phase table, add new features

---

## IMPLEMENTATION ORDER

1. `scripts/repo_audit.py` — core data layer changes first
2. `skills/autodebug.md` — the main skill prompt (biggest change)
3. `scripts/test_skill.py` — update validation
4. `CHEETAHCLAWS.md` — update project context
5. `README.md` — update docs

Then run `python3 scripts/test_skill.py` to validate everything passes.
